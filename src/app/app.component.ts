import { Component } from "@angular/core";

@Component({
  selector: "app-root",
  templateUrl: "./app.component.html",
  styleUrls: ["./app.component.css"],
})
export class AppComponent {
  // We removed ngOnInit and all cookie functions to keep it clean.
  // This component now only serves as the main shell for your app.
}